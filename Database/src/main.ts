import { addProduct, showProducts } from "./products";
import { Product } from "./models";

const product:Product = {
    name: 'Apple',
    price:1200,
    count:13,
    color:'gold'
}

// addProduct(product)
showProducts()